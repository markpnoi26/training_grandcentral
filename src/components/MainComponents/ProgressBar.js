import React from "react";
import Container from "react-bootstrap/Container";

const ProgressContainer = (props) => {
    return (
        <Container>
            I am the Progress Container Component
        </Container>
    )
};

export default ProgressContainer;
